# GPU-Accelerated Cross-Dataset Testing Requirements
# For NVIDIA GPUs with CUDA support

# Core GPU libraries
cupy-cuda12x>=12.0.0  # Use cupy-cuda11x for CUDA 11.x
cudf-cu12>=23.10.0    # RAPIDS cuDF for GPU DataFrames
cuml-cu12>=23.10.0    # RAPIDS cuML for GPU ML algorithms

# XGBoost with GPU support
xgboost>=2.0.0        # Ensure GPU support is compiled

# Standard ML libraries (CPU fallback)
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars and utilities
tqdm>=4.65.0

# Installation Instructions:
# 1. Install CUDA Toolkit 12.x from NVIDIA
# 2. Install cuDNN
# 3. Install RAPIDS: conda install -c rapidsai -c conda-forge -c nvidia rapids=23.10 python=3.10 cuda-version=12.0
# 4. Or use pip: pip install -r requirements_gpu.txt
#
# For CUDA 11.x, replace:
#   cupy-cuda12x -> cupy-cuda11x
#   cudf-cu12 -> cudf-cu11
#   cuml-cu12 -> cuml-cu11
